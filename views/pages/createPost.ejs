<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Write — DevBlog</title>
    <style>
        * { margin:0; padding:0; box-sizing:border-box; }
        body { background:#f8f8f2; color:#2f4f4f; font-family:Arial, sans-serif; }
        .container { max-width:700px; margin:40px auto; padding:0 20px; }
        h1 { color:#2e8b57; margin-bottom:32px; }
        input, textarea { width:100%; padding:14px; margin-bottom:20px; background:#f9f9f9; border:1px solid #ddd; border-radius:8px; color:#2f4f4f; font-size:1rem; }
        textarea { height:300px; resize:vertical; }
        button { padding:12px 32px; background:#2e8b57; border:none; border-radius:8px; color:white; font-size:1rem; cursor:pointer; }
        button:hover { background:#228b22; }
        .error { background:#ffe6e6; border:1px solid #daa520; padding:10px; border-radius:8px; margin-bottom:16px; color:#800020; }
    </style>
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container">
        <h1>Write a Post</h1>

        <% if (error) { %>
            <div class="error"><%= error %></div>
        <% } %>

        <form action="/post/create" method="POST">
            <input type="text" name="title" placeholder="Post title..." required />
            <textarea name="content" placeholder="Write your post here..."></textarea>
            <button type="submit">Publish</button>
        </form>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>